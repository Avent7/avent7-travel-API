export enum BookingStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  REFUNDED = 'refunded',
  FAILED = 'failed',
}

export enum Provider {
  DUFFEL = 'duffel',
  RATEHAWK = 'ratehawk',
  HOTELBEDS = 'hotelbeds',
  VIATOR = 'viator',
  MANUAL = 'manual',
}
