/**
 * Categorias de fornecedor — espelham o `BlockType` do build da proposta
 * (avent7-travel/app/features/propostas/blockTypeUtils.ts).
 */
export enum SupplierCategory {
  AEREO = 'aereo',
  HOSPEDAGEM = 'hospedagem',
  TRANSPORTE = 'transporte',
  EXPERIENCIA = 'experiencia',
  RESTAURANTE = 'restaurante',
}
